<%= form_with(model: [:admin, @test, @question], local: true) do | form | %>
  <%= render 'shared/errors', resource: @questions %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body, id: :question_body %>
  </p>

  <p> <%= form.submit %> </p>
<% end %>
